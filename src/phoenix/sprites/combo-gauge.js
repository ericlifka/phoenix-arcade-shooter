DefineModule('phoenix/sprites/combo-gauge', function (require) {
    var Sprite = require('models/sprite');

    return function () {
        var n = null;
        return new Sprite([
            []
        ]);
    };
});
